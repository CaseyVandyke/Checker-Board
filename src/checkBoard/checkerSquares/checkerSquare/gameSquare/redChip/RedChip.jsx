import React from "react";
import PropTypes from "prop-types";

const RedChip = props => {
  return (
      <div className="red chip" />
  );
};

RedChip.propTypes = {

};

RedChip.defaultProps = {

}

export default RedChip;